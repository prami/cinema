<%= form_for(@showing) do |f| %>
  <% if @showing.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@showing.errors.count, "error") %> prohibited this showing from being saved:</h2>
    <ul>
    <% @showing.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :film_id %><br />
    <%= collection_select(:showing, :film_id, Film.order(:title), :id, :title_with_initial) %>
  </div>
  <div class="field">
    <%= f.label :Sala %><br />
    <%= collection_select(:showing, :room_id, Room.order(:name), :id, :name_with_initial) %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.time_select :time %>
  </div>
  <div class="field">
    <%= f.label :Dodatkowe_informacje %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
